# 2026-06-01 — Phase 2 DX improvements

**Branch:** `feat/phase-2-dx`
**Plan steps completed:** 7, 9, 10

---

## Step 7 — Prettier

- Added `.prettierrc` (singleQuote, semi, trailingComma all, printWidth 100)
- Added `.prettierignore` (excludes node_modules, .next, dist, migrations, pnpm-lock.yaml)
- `format` script was already wired in package.json from Step 12
- First run reformatted 105 files to establish baseline style

## Step 9 — Husky + lint-staged

- Installed `husky` + `lint-staged` at workspace root
- Initialized `.husky/pre-commit` → runs `npx lint-staged`
- `prepare` script auto-installs husky hooks on `pnpm install`
- lint-staged config in root `package.json`:
  - `apps/api/src/**/*.ts` → eslint --fix + prettier --write
  - `apps/web/**/*.{ts,tsx}` → prettier --write
  - `packages/**/*.ts` → prettier --write
  - `*.{json,md}` → prettier --write

## Step 10 — Zod env validation (frontend)

- Created `apps/web/lib/env.ts` — validates `NEXT_PUBLIC_API_URL` at startup via Zod
- Replaced all 5 `process.env.NEXT_PUBLIC_API_URL` usages with `env.NEXT_PUBLIC_API_URL`:
  - `lib/api/client.ts`
  - `lib/store/auth.store.ts`
  - `app/(account)/compte/connexion/page.tsx`
  - `app/(account)/compte/inscription/page.tsx`
  - `app/auth/callback/page.tsx`
- If `NEXT_PUBLIC_API_URL` is missing or not a valid URL, the app now throws at startup with a clear Zod error instead of a cryptic Axios failure

## Verification

- `pnpm --filter @unitree/web typecheck` — passes
- `pnpm --filter @unitree/web lint` — passes

## Next step

Sprint 4: `/checkout` page — Stripe PaymentElement checkout for accessories.
